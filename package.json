{
  "name": "sicot",
  "version": "2.0.0",
  "scripts": {
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
